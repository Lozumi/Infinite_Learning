/*
 * 选手作答区，提交本文件的内容到评测系统。
 */

#include "rover_controller.h"

void init(float maxLinearVel, float maxAngVel, int fullScoreMaxTries, int maxTries)
{
}

void navigateTo(float x, float y, float radius)
{
    // TODO Do something here
}

CmdVel update(float x, float y, float ang)
{
    CmdVel result;
    result.linear = .0f;
    result.angular = .0f;

    //检查朝向
    int cAng = acos(abs())
    result.angular = acos
}
